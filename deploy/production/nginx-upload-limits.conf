# Included by /etc/nginx/nginx.conf inside the http block on the production host.
# Keep this in sync with the largest application-level upload limit (learning files: 50MB).
client_max_body_size 50m;
